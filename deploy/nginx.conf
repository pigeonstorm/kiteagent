# KiteAgent Nginx TLS config snippet
# Add to your server block for HTTPS (required for Web Push service workers)
#
# server {
#     listen 443 ssl http2;
#     server_name kite.yourdomain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/kite.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/kite.yourdomain.com/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }
